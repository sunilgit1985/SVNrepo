<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://camel.apache.org/schema/spring
http://camel.apache.org/schema/spring/camel-spring.xsd">

    <bean id="propertyPlaceholderConfigurer"
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
               <value>classpath:sql.properties</value>
               <value>classpath:ftp.properties</value>
            </list>
        </property>
    </bean>

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

   <!-- configure the Camel SQL component to use the JDBC data source -->
   <bean id="sql" class="org.apache.camel.component.sql.SqlComponent">
        <property name="dataSource" ref="dataSource"/>
    </bean>

   <bean id="dbBean" class="com.invessence.broker.dao.DatabaseBean" init-method="initialize">
      <property name="dataSource" ref="dataSource"/>
   </bean>

    <bean id="dateFilter" class="com.invessence.broker.filters.DateFilter">
      <property name="databaseBean" ref="dbBean"/>
   </bean>

   <bean id="processingConfig" class="com.invessence.broker.dao.FileProcessingConfig">
      <property name="dataSource" ref="dataSource"/>
      <property name="dateFilter" ref="dateFilter"/>
   </bean>

   <bean id="fileProcessor" class="com.invessence.broker.processors.FileProcessor" destroy-method="postProcess">
      <constructor-arg index="0" value="${ftp.localdir}"/>
      <constructor-arg index="1" ref="processingConfig"/>
      <constructor-arg index="2" ref="dataSource"/>
      <constructor-arg index="3" value="${report.localdir}"/>
   </bean>

   <camelContext id="camel-context" xmlns="http://camel.apache.org/schema/spring">
   <!-- use Camel property placeholder loaded from the given file -->
      <propertyPlaceholder id="ftpProperties" location="classpath:ftp.properties,classpath:sql.properties"/>

      <!-- Process Position Files-->
      <route id="ftpPositionFilesFromBroker">
         <from
            uri="ftp://{{ftp.username}}@{{ftp.host}}/Outgoing?password={{ftp.password}}&amp;filter=#processingConfig&amp;delay={{ftp.delay}}&amp;recursive=false"/>
         <to uri="bean:fileProcessor?method=process"/>
      </route>
   </camelContext>
</beans>